{
  "kind": "collectionType",
  "collectionName": "pedidos",
  "info": {
    "singularName": "pedido",
    "pluralName": "pedidos",
    "displayName": "Pedido",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "fechaPedido": {
      "type": "datetime",
      "required": true
    },
    "cliente": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::cliente.cliente"
    },
    "estado": {
      "type": "enumeration",
      "enum": [
        "pendiente",
        "completo",
        "cancelado"
      ],
      "default": "pendiente",
      "required": true
    },
    "Productos": {
      "type": "json",
      "required": true
    }
  }
}
