{
  "kind": "collectionType",
  "collectionName": "pedidos",
  "info": {
    "singularName": "pedido",
    "pluralName": "pedidos",
    "displayName": "Pedido",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "fechaPedido": {
      "type": "datetime",
      "required": true
    },
    "cliente": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::cliente.cliente",
      "inversedBy": "pedidos"
    },
    "estado": {
      "type": "enumeration",
      "enum": [
        "pendiente",
        "completo",
        "cancelado"
      ],
      "default": "pendiente",
      "required": true
    },
    "Productos": {
      "type": "json",
      "required": true
    }
  }
}
